# MetricStat

The loudest element in any listing: a Fraunces number in verdigris with a small mono caption.

```jsx
<MetricStat value="81.8% → 85.1%" label="TRANSACTION SUCCESS" size="xlarge" />
```

pending renders a dashed TBD box instead of the number. One stat per project, never two stacked.
