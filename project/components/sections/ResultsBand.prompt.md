# ResultsBand

Centred single-stat band closing a case study.

```jsx
<ResultsBand value="700K+ installs" caption="Installs in the first year after relaunch." />
```

One number only. Rules top and bottom, plus an internal hairline above the caption.
