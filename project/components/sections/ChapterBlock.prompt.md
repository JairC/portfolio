# ChapterBlock

Case-study chapter: inline number and label rail at left, heading and body at right.

```jsx
<ChapterBlock number="01" label="CONTEXT" heading="Chapter heading" active />
```

Rail is 120px wide with a 100px gap. The active chapter number is red, the rest are --line-strong. Body maxes at 760px. The rail is inline, never sticky.
